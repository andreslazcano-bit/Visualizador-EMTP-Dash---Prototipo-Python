# Core Dash
dash==2.14.2
dash-bootstrap-components==1.5.0
dash-auth==2.0.0

# Data Processing
pandas>=2.2.0  # Compatible con Python 3.13
numpy>=1.26.0
# polars==0.20.3  # Comentado temporalmente para simplificar instalación

# Visualización
plotly==5.18.0
# folium==0.15.1  # Comentado: no esencial para la app actual

# Mapas y Geoespacial (comentados para evitar compilación en hosts sin librerías del sistema)
# geopandas==0.14.1
# shapely==2.0.2
# pyproj==3.6.1

# Bases de Datos
sqlalchemy==2.0.23
# pyodbc==5.0.1  # SQL Server (comentado: requiere compilación)
# psycopg2-binary==2.9.9  # PostgreSQL (comentado para despliegues en hosts sin pg_config)
# pymysql==1.1.0  # MySQL (comentado: no esencial para demo)
# pymongo==4.6.1  # MongoDB (comentado: no esencial para demo)

# SharePoint Integration (comentado: no esencial para demo con datos locales)
# Office365-REST-Python-Client==2.5.3
# shareplum==0.5.1

# Excel/CSV
openpyxl==3.1.2
# xlrd==2.0.1  # Comentado: librería legacy
pyarrow>=14.0.0  # Para archivos Parquet, compatible con Python 3.13

# Autenticación y Seguridad
bcrypt==4.1.2
python-dotenv==1.0.0
pyjwt==2.8.0

# Generación de Reportes
# reportlab==4.0.7  # PDFs (comentado: no esencial para demo)
# python-docx==1.1.0  # Word (comentado: no esencial para demo)
jinja2==3.1.2

# Utilidades
python-dateutil==2.8.2
pytz==2023.3.post1
requests==2.31.0

# Cache (comentado: no esencial para demo)
# redis==5.0.1
# flask-caching==2.1.0

# Logging
loguru==0.7.2

# Production Server
gunicorn==21.2.0  # Para deployment en Render/Railway/Heroku

# Testing (opcional, ver requirements-dev.txt)
# pytest==7.4.3
# pytest-cov==4.1.0
